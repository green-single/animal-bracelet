# 🐾 二维码管理清单（公网版）

所有二维码内容均为 `https://animal-bracelet.onrender.com/c/{码}`，共 65 个。

## 真实数据动物（推荐优先使用）

### Noé · 白鹳

`325H-TJS4`（unused）, `6XJU-2FH8`（unused）, `9MAN-RN5H`（unused）, `ANJ4-XAQH`（unused）, `BQQT-ZSMA`（unused）, `CZE5-FE6J`（unused）, `FTZA-4XFA`（unused）, `HA46-B4VZ`（unused）, `HBF2-WG7M`（unused）, `X7VM-FZQ6`（unused）

### Lumi · 欧斑鸠

`B5UP-UK5R`（unused）, `CKQP-M8YR`（unused）, `QHVW-CR4J`（unused）, `SG2J-AJ4K`（unused）, `UMMZ-S9AF`（unused）

### Athos · 红鸢

`4XMI-L5X7`（unused）, `664A-V2YY`（unused）, `BPG6-BC5H`（unused）, `LKF8-G3C4`（unused）, `MSK0-4T9S`（unused）

### Mel · 蜂鹰

`02HO-3QHX`（unused）, `1SUW-YVMI`（unused）, `ELU0-N1U3`（unused）, `ETXK-Q8S2`（unused）, `MOMT-1O2K`（unused）

### Silver · 银鸥

`3VAI-SPJA`（unused）, `42AB-Z0ZB`（unused）, `4A1Z-C6P5`（unused）, `AWUW-WQ7K`（unused）, `XSZT-0T5J`（unused）

## 示例/旧数据动物（不建议对外发）

### Noel · 绿海龟(2011)

`5AYP-3554`（unused）, `5EBQ-TRN6`（unused）, `8TS2-3RXR`（unused）, `EPXE-HHRE`（unused）, `ESZK-4MQA`（unused）, `JV6Q-MSNX`（unused）, `PGNF-BYZK`（unused）, `RQ52-JQDB`（unused）, `TCAH-VFZN`（unused）

### Kaos · 座头鲸(1点)

`BN7F-VC64`（unused）, `ECQD-ECBN`（unused）, `ENWM-M5VG`（unused）, `JYZ3-6QWX`（unused）, `MYP4-EHK7`（unused）, `PARA-EQZ5`（unused）, `SZEW-DYAD`（unused）, `TFMK-KKNJ`（unused）, `UAVY-N6GF`（unused）, `XM62-K4GV`（unused）

### Koa · 大白鲨(示例)

`5BHR-K99X`（unused）, `MMMD-579M`（unused）, `UPNC-ZXZQ`（unused）, `YEM4-3HCA`（unused）

### Nuna · 北极燕鸥(示例)

`4SYS-R6F8`（claimed）, `8CHV-YM2D`（unused）, `9GB6-VFF9`（unused）, `KHYM-NVWZ`（unused）

### Pippin · 帝企鹅(示例)

`9WH8-6W3H`（unused）, `JHMA-EMXG`（unused）, `UVG7-MUUY`（unused）, `VDDN-8VQM`（unused）

### Tembo · 非洲象(示例)

`2SKS-SPTN`（unused）, `FC2U-RTD8`（unused）, `R364-858K`（unused）, `SYVP-WW9T`（unused）

---
打印稿：`data/print_sheets/二维码打印稿_A4_公网版.pdf`（7页A4）